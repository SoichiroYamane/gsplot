# 5. Scatter plots

`gsplot.scatter` returns a native `PathCollection`. `gsplot.cmap_scatter`
adds explicit finite scalar values for colormap mapping.

```{literalinclude} ../../../demo/5_scatter/scatter.py
```

```{image} ../../../demo/5_scatter/scatter.png
:alt: Explicit scatter and colormap scatter plots
:class: bg-primary
:width: 1000px
:align: center
```
